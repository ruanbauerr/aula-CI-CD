name: Pipeline test

on:
  push:
    branches:
      - dev

jobs:
  validar-push-dev:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verificar index.html
        run: test -f index.html